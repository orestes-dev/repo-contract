# The composite action runs `node src/action.js` on the consumer's runner and
# imports `yaml` at runtime, so dependencies must resolve as a plain
# node_modules tree (not PnP). action.yml installs them, cached, before the run.
nodeLinker: node-modules
